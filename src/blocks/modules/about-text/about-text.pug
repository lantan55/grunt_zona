section.section.about-text
    .container.about-text__wrapper
        p.about-text__subtitle #{aboutTextSubtitle}
    .container.about-text__cards
        if aboutTextList
            each item in aboutTextList
                img(src=item, alt="")
